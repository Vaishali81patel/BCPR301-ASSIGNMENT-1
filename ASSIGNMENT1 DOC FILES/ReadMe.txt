
To test functionality:

Run ***** main.py *****


commands that can be invoked from prompt or on the command line are:
       >>> read <filename>
               
valid extensions: db(SQLite), csv, pickle
or     

>>> graph <filename> [option]
               valid options: pie
               default option: bar_chart
               

note: only shows Male Female ratio
or     

>>> save <filename>
               
filename must not be already a file
  valid extensions: db(SQLite), csv, pickle
or     >>> show
               
if no file name given then the records read
   (using read command) are shown
       >>> show <filename>
               
filename must be an existing file
             valid extensions: db(SQLite), csv, pickle

To see help available:
at prompt: ? 

